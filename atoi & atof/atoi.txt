#include<stdio.h>
int main(){
	char s[10];
	printf("enter str:");
	scanf("%s",s);
	int i=0,flag=1,sum=0,num=0;
	if(s[i]=='+'){
		flag=1;
		i=1;
	}
	else if(s[i]=='-'){
		flag=-1;
		i=1;
	}
	for(;s[i];i++){
		if(s[i]>='0' && s[i]<='9'){
			sum=sum*10+(s[i]-'0');
		}
else
break;
	}
	num=sum*flag;
	printf("%d",num);
}
